@using IntexLegoSecure.Infrastructure
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model Product
<link rel="stylesheet" href="~/css/Style.css" />

<div>

    <section class="products" id="products">
        @* <h1 class="heading">Our <span>Products</span> </h1> *@

        <div class="box-container">

            <div class="box">

                <div class="image">
                    <img src="@Model.ImgLink" />
                    <div class="icons">
                        @* This was in here before. *@
@*                         <form method="post" asp-page="/Cart" class="icons" id=@Model.ProductId>
                            <input type="hidden" asp-for="ProductId" />
                            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                            <a href="#" class="cart-btn"><span class="fas fa-shopping-cart"></span></a>
                        </form> *@
                        @* This was in here before. *@
@*                         <form method="post" asp-page-handler="AddToCart" asp-route-productId="@Model.ProductId">
                            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                            <button type="submit" class="cart-btn"><span class="fas fa-shopping-cart"></span></button>
                        </form> *@
                        @* I added this because it worked in the other project. *@

@*                         <form id=@Model.ProductId method="post" asp-page="/Cart">
                            <input type="hidden" asp-for="ProductId" />
                            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" /> *@

@*                         <form id=@Model.ProductId method="post" asp-page="/Cart" class="icons">
                            <input type="hidden" asp-for="ProductId" />
                            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                            <a class="cart-btn"><span class="fas fa-shopping-cart"><button type="submit">Hello</button></a>
                        </form> *@
                        <form id=@Model.ProductId method="post" asp-page="/Cart">
                            <input type="hidden" asp-for="ProductId" />
                            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                            <button type="submit" class="btn-outline-success btn-sm float-end">Add to cart</button>
                        </form>
                    </div>
                </div>
                <div class="content">
                    <h3>@Model.Name</h3>
                    <div class="price">@Model.Price</div>
                </div>
            </div>

        </div>
    </section>
</div>







